make Morris dialog work
    -so far, only loads factor names and levels - all other functions are still missing
    -make sure level ordering is correct (numerical, not alphabetical, for quantitative factors!)

load/save Morris settings

How to set up low pass filter in this case?

testing

make functions that are marked with ** (dev version only) invisible

write new tutorial and trim away unused functions, e.g. the Morris analysis and regions and aois
    
publish, add comments to GEB and JORS?